@using Abogado.Domain.Entities
@model List<HistoricoCaso>
@{
    ViewData["Title"] = "Login";
}

<div class="jumbotron jumbotron-fluid">
    <div class="container">
        <h1 class="display-4">Observe el historico de los casos</h1>
        <p class="lead">informacion general</p>
    </div>
</div>

@if (Model.Count != 0)
{
    <table class="table table-dark">
        <thead>
            <tr>
                <th scope="col">Nombre</th>
                <th scope="col">forma</th>
                <th scope="col">mecanismo</th>
                <th scope="col">proceso</th>
                <th scope="col">descripcion</th>
                <th scope="col">Fecha</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var historico in Model)
            {
            <tr>
                <td>@historico.NombreCaso</td>
                <td>@historico.FormaDivorcio</td>
                <td>@historico.mecanismoDisolucion</td>
                <td>@historico.Proceso</td>
                <td>@historico.Descripcion</td>
                <td>@historico.Fecha</td>
            </tr>
            }
        </tbody>
    </table>

}
else
{
    <div class="card">
        <div class="card-header">
            en este momento no tiene historicos
        </div>
        <div class="card-body">
            <h5 class="card-title">Sin Registros aun</h5>
            <p class="card-text">Aun no tiene historicos, debes crear para comenzar</p>
            <a class="btn btn-outline-dark" asp-area="" asp-controller="Home" asp-action="Index">retornar</a>
        </div>
    </div>
}
